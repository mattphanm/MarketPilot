# Expand the domain to match the dashboard export

We decided the Figma dashboard integration is a product-domain expansion, not only a visual refresh. The implementation should persist the core concepts shown in the export where they affect real workflows, while still treating purely presentational dashboard aggregates as derived analytics.
