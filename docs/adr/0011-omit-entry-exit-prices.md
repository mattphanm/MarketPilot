# Omit entry and exit prices from the first futures journal model

MarketPilot does not collect entry or exit prices in the first futures-focused implementation. Because the product does not track contracts, entry and exit prices cannot produce realized P&L; the core outcome is instead calculated from risk dollars and R multiple.
