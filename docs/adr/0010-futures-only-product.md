# Make MarketPilot futures-only

MarketPilot is scoped to completed futures trades, not stocks, ETFs, options, or crypto. The Figma export's asset-type selector should not be copied into the product because it would add unused concepts and validation paths.
