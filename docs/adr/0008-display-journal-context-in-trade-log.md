# Display journal context in the trade log

Trade idea and confluences are stored once on the journal entry, but displayed in both the Journal tab and the Trade Log detail drawer. This matches the Figma interaction while avoiding duplicated fields that could drift between `Trade` and `JournalEntry`.
