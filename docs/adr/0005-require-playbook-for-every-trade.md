# Require a playbook for every trade

Every completed trade in MarketPilot must reference a playbook. This makes the Playbooks tab a real product feature instead of a decorative grouping layer, and ensures playbook performance can be derived consistently from logged trade outcomes.

The add-trade workflow must allow creating a playbook inline when none exists or when the user needs a new strategy, then automatically assign the new trade to that playbook.
