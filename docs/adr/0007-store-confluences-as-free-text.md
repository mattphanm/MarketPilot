# Store confluences as free text

MarketPilot initially stores confluences as one free-text field instead of normalized tags or chips. This matches the first trade-log workflow and keeps the model flexible until real usage shows whether confluences need structured analytics.
