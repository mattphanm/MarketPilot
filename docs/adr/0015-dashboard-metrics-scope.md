# Scope dashboard metrics to journal outcomes

The first Figma integration should use the dashboard visual system but only display metrics supported by the post-trade futures journal model: net P&L, win rate, total trades, average R, average win, average loss, profit factor, daily P&L, equity curve, and playbook performance. Portfolio value, cash balance, sector allocation, open positions, and unrealized gains are intentionally out of scope.
