# Include the Figma settings tab

MarketPilot will include a Settings tab in the Figma-aligned app shell. The tab should match the export's visual direction, but should only expose real account, sign-out, and workspace information until preference features are intentionally added.
